# `docs/` — app documentation

This directory is reserved for documentation that directly explains the React application in this repository.

At the moment, the directory contains this README only. The broader application-delivery playbook and its worked artifacts are intentionally maintained separately rather than mixed into this React app repository.

If app-specific documentation is added here, keep it close to the source it explains, link it from this README, and verify that its routes, file names, component behavior, and setup instructions match the implementation.

Return to the [repository README](../README.md) for setup, routes, structure, and verification.
